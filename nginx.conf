server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index cloudreve.html;

    location = / {
        return 302 /website/index.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
